# CloudDisk V2 — 各模块实现细节（面试用）

## 整体架构

```
Client → OpenResty (:2024) → C++ Gateway (Drogon) → Go gRPC 微服务
                                ├── account_srv — 注册/登录/JWT
                                ├── file_srv    — 文件上传/下载/删除/分片上传
                                ├── AI_srv      — AI 功能
                                └── mcp_server  — MCP 工具服务

file_srv → Kafka (Outbox 模式) → store_srv (消费者) → MinIO/AliOSS
email_srv (C++) ← Kafka — 邮件通知

基础设施：Nacos(配置中心) + Consul(服务发现) + Kafka(异步消息) + MySQL + Redis + MinIO/AliOSS
部署：Kubernetes
```

---

## 一、OpenResty 网关层

### 1.1 职责

Nginx + Lua 实现的反向代理，负责路由分发、CORS、静态资源服务、请求追踪。

### 1.2 动态服务发现

```lua
-- init_worker 阶段每 5 秒从 Consul 拉取健康实例
local Service = require("lua.consul_service")
-- GET /v1/health/service/{service}?passing=true
-- 解析 JSON，提取 Service.Address:Service.Port
-- 存入 lua_shared_dict consul_services (10MB 共享内存)
```

### 1.3 负载均衡

```lua
-- balancer_by_lua 阶段
-- 基于 request_id 的一致性哈希：crc32(request_id) % server_count
-- 单实例时直接选中
```

### 1.4 请求追踪

```lua
-- access_by_lua 阶段
-- 优先使用客户端传入的 X-Request-Id
-- 否则生成：timestamp-worker_pid-connection-request_count
-- 注入请求头转发给后端
```

### 1.5 CORS 处理

- 全局添加 `Access-Control-Allow-Origin: *`
- OPTIONS 预检请求直接返回 204
- 允许 Content-Type, Authorization, X-Requested-With

### 1.6 静态资源

- 根目录指向 `forward_part/static/dist`（前端构建产物）
- JS/CSS/图片等静态文件缓存 1 年（`Cache-Control: public, immutable`）
- SPA fallback：`try_files $uri $uri/ /index.html`
- 端口 2025 提供文件预览（inline）和下载（attachment）

---

## 二、C++ Gateway（Drogon 框架）

### 2.1 职责

HTTP → gRPC 协议转换，JWT 鉴权，服务发现缓存，请求过滤。

### 2.2 启动流程

```
1. GetFreePort() 获取可用端口
2. 创建 Redis 客户端（Token 管理）
3. 从 Nacos 获取配置（Kafka/Consul/JWT 密钥）存入 MyAppData 单例
4. 注册到 Consul
5. 启动 Drogon HTTP 服务
```

### 2.3 JWT 鉴权机制

**Token 生命周期管理（Redis）：**

```
签发时：
  1. ZADD jwt:whitelist:user:{userId} {timestamp} {token}  // 加入白名单有序集合
  2. ZCARD 检查数量，超过 2 个时 ZPOPMIN 移除最旧的
  3. 被移除的 token SADD 到 jwt:blacklist

验证时（jwt_decode 过滤器）：
  1. 从 Authorization: Bearer {token} 提取 token
  2. HS256 解码，验证 issuer == "Signin"
  3. SISMEMBER jwt:blacklist 检查是否已注销
  4. ZSCORE jwt:whitelist:user:{userId} 检查是否在白名单
  5. 通过后将 ID、Name 存入请求 attributes

登出时：
  1. 从白名单 ZREM 移除
  2. SADD 加入黑名单
```

**设计亮点：** 每用户最多 2 个活跃 token（多设备登录），超出自动踢掉最早的。

### 2.4 ARC 缓存（自适应替换缓存）

**用途：** 缓存 Consul 服务发现结果（gRPC 连接 + Stub），容量 20。

**算法实现：**
- LRU + LFU 混合缓存，自适应分区
- LRU 部分：双向链表，按访问时间排序
- LFU 部分：频率映射 `map<size_t, list<NodePtr>>`，追踪最小频率
- Ghost Cache：记录被淘汰的 key，命中时动态调整 LRU/LFU 容量比例
- 访问次数超过阈值（2次）时，从 LRU 提升到 LFU

**面试要点：** 相比纯 LRU 或 LFU，ARC 能自适应工作负载模式——频繁访问的服务走 LFU 路径，偶尔访问的走 LRU 路径。

### 2.5 gRPC 客户端调用

- 从 Consul 发现服务实例，维护 gRPC Channel 列表
- 原子计数器实现 Round-Robin 负载均衡
- Channel 健康检查：`GetState()` 返回 READY/IDLE/CONNECTING
- 懒初始化：首次使用时创建 Channel
- TTL 缓存过期（默认 10 秒）

**分片上传的流式 gRPC：**
```cpp
// UploadPartReactor 继承 grpc::ClientWriteReactor<file::UploadPartReq>
// Stage 0: 发送元数据（upload_id, part_number）
// Stage 1: 发送二进制数据块
// Stage 2: 关闭写入流
```

### 2.6 API 路由

| 路径 | 方法 | 功能 | 鉴权 |
|------|------|------|------|
| `/user/signup` | POST | 注册 | 无 |
| `/user/signin` | POST | 登录 | 无 |
| `/user/info` | GET | 用户信息 | JWT |
| `/user/sendcode` | POST | 发送验证码 | 无 |
| `/user/code` | POST | 验证码校验 | 无 |
| `/user/token/blacklist` | POST | 登出 | JWT |
| `/file/query` | POST | 文件列表 | JWT |
| `/file/upload` | POST | 单文件上传 | JWT |
| `/file/download` | POST | 获取下载URL | JWT |
| `/file/showfile` | POST | 获取预览URL | JWT |
| `/file/delete` | POST | 删除文件 | JWT |
| `/file/initupload` | POST | 初始化分片上传 | JWT |
| `/file/uploadpart` | POST | 上传分片 | JWT |
| `/file/CompleteMultipart` | POST | 完成分片上传 | JWT |
| `/file/AbortMultipart` | POST | 取消分片上传 | JWT |
| `/file/Status` | POST | 上传进度查询 | JWT |
| `/AI` | POST | AI 请求 | JWT |
| `/health` | GET | 健康检查 | 无 |

---

## 三、Account 服务（Go gRPC）

### 3.1 启动流程

```
1. net.Listen("tcp", ":0") 动态获取可用端口
2. 创建 gRPC Server，注册 AccountService
3. 注册 gRPC Health Check（供 Consul 探活）
4. 注册到 Consul（account_srv_1）
5. 启动 Serve
6. 监听 SIGINT/SIGTERM，优雅退出时从 Consul 注销
```

### 3.2 注册流程（Signup）

```
1. 查询 username 是否已存在（Name 字段有唯一索引）
2. 密码加密：
   - 库：go-password-encoder（PBKDF2）
   - 参数：SaltLen=16, Iterations=100, KeyLen=32, HashFunction=MD5
   - 生成 salt + encodedPassword（均为 base64）
3. 创建 Account 记录（salt、password、username、email）
4. 返回 code=0 表示成功
```

### 3.3 登录流程（Signin）

```
1. 按 username 查询 Account
2. password.Verify(输入密码, 存储的salt, 存储的hash, options)
3. 生成 JWT Token：
   - 算法：HS256
   - Claims：ID(用户ID), Name(用户名), ExpiresAt(24小时), Issuer("Signin")
   - 签名密钥：来自 Nacos 配置
4. 返回 token（code=0, message=token字符串）
```

### 3.4 JWT 实现细节

```go
// 单例模式（sync.Once）
type JWT struct { SigningKey []byte }
type CustomClaims struct {
    jwt.StandardClaims
    ID   int32   // 用户 ID
    Name string  // 用户名
}

// Token 刷新的巧妙实现：
// 临时将 jwt.TimeFunc 设为 Unix(0,0) 绕过过期检查
// 解析出 claims 后生成新 token，再恢复 TimeFunc
```

### 3.5 错误码设计

| Code | 含义 |
|------|------|
| 0 | 成功 |
| 1 | 账户不存在 / 已存在 |
| 2 | 密码错误 / 数据创建出错 |
| 3 | Token 错误 |

错误消息使用中文常量：`"账户不存在"`, `"密码错误"` 等。

---

## 四、File 服务（Go gRPC）

### 4.1 单文件上传（LoadFile）

```
1. 事务内操作：
   a. 验证用户存在
   b. File 表 Upsert（OnConflict DoNothing）—— SHA1 去重
   c. 创建 UserFile 关联（account_id + file_id + filename 三字段联合唯一索引）
2. 如果文件已 READY（status="success"）：直接返回，无需重复上传
3. 否则：写入 Outbox 记录，异步通过 Kafka 上传到 OSS
   - Payload：TxID, FileID, SHA1, Size, Content(字节), MIME Type
```

**SHA1 去重机制：** File 表的 sha1 字段有唯一索引，多个用户上传相同内容的文件只存储一份，通过 UserFile 表建立多对多关系。

### 4.2 分片上传

**初始化（InitMultipart）：**
```
1. 生成 uploadID（16字节随机 hex）
2. 构建 objectKey：u/{user_id}/{file_hash}
3. minio.OptimalPartInfo() 计算最优分片大小和总片数
4. MinIO 发起 multipart upload，获取 ossUploadID
5. Redis Hash 存储元数据：mp:meta:{uploadID}
   字段：user_id, file_name, file_size, file_hash, content_type,
         object_key, part_size, total_parts, oss_upload_id, status
   TTL：24 小时
```

**上传分片（UploadPart，客户端流式 gRPC）：**
```
1. 第一条消息：Meta（uploadID, partNumber, partSize, chunkHash）
2. 后续消息：Data 数据块
3. 写入临时文件（避免内存膨胀）
4. 校验接收大小 == meta.PartSize
5. MinIOMultipartPutPart() 上传到 MinIO，获取 ETag
6. Redis Hash 存储：mp:parts:{uploadID}[partNumber] = etag
```

**完成（CompleteMultipart）：**
```
1. 从 Redis 获取 meta 和所有 part ETag
2. 验证已上传片数 == totalParts
3. MinIO CompleteMultipartUpload()（parts 按 PartNumber 排序，S3 要求）
4. 更新 Redis meta status = "completed"
```

**取消（AbortMultipart）：**
```
1. MinIO AbortMultipartUpload() 清理已上传的分片
2. 删除 Redis 的 meta 和 parts 两个 Hash（幂等操作）
```

### 4.3 Outbox 模式（异步可靠投递）

**OutboxDispatcher：**
```
- 每 2 秒轮询数据库
- 批量大小：50 条
- 8 个并发 Worker
- 分布式锁 TTL：30 秒

流程：
1. claimBatch()：SELECT ... FOR UPDATE SKIP LOCKED
   - 筛选 status=NEW/FAILED 且 next_retry_at <= now
   - 更新 status=SENDING，设置 locked_by 和 locked_until
2. Worker 发送到 Kafka
3. 成功：markSent() → status=SENT，清除锁
4. 失败：markFailed() → retry_count++，指数退避（2^n 秒，上限 5 分钟）
```

**面试要点：**
- `FOR UPDATE SKIP LOCKED` 避免多实例争抢同一条记录
- 指数退避防止失败消息频繁重试
- Outbox 保证业务数据和消息在同一个数据库事务中，解决分布式事务问题

### 4.4 文件下载（Filedowm）

```
1. 验证用户存在
2. 查询文件状态 == "success"（READY）
3. MinIO 生成预签名 URL（10 分钟有效期）
4. Content-Disposition: attachment; filename*=UTF-8''{filename}
```

### 4.5 文件删除（DeleteFile）

```
1. 查找 UserFile（account_id + filename + filehash）
2. 删除 UserFile 关联
3. 检查是否还有其他用户引用该 File
4. 如果无引用：删除 File 记录 + MinIO 对象（best-effort）
```

---

## 五、Store 服务（Kafka 消费者）

### 5.1 消费者配置

```
Broker：192.168.149.128:31092
Consumer Group：store_srv_group
Topic：file.upload.cmd
分区策略：Range
自动提交：开启（1 秒间隔）
初始偏移：Newest
```

### 5.2 Worker Pool 模型

```
ConsumeClaim() → Task Channel (workerCount*2 缓冲) → N 个 Worker 协程
                                                        ↓
                                                   processMessage()
                                                        ↓
                                                   成功：MarkMessage()
                                                   失败：不提交（Kafka 重投递）
```

Worker 数量默认 10，任务队列缓冲 20。

### 5.3 Inbox 幂等消费（三阶段获取锁）

```
Phase 1 — 插入新记录：
  INSERT Inbox(event_id, status=PROCESSING, locked_by=instanceID, locked_until=now+2min)
  成功 → 获得锁，开始处理

Phase 2 — 记录已存在：
  查询现有记录
  如果 status=DONE → 跳过（已处理过）

Phase 3 — 锁接管：
  UPDATE Inbox SET status=PROCESSING, locked_by=instanceID
  WHERE event_id=? AND status<>DONE AND (locked_until IS NULL OR locked_until < now)
  RowsAffected > 0 → 获得锁
```

**instanceID = hostname + PID**，2 分钟锁超时防止死锁。

### 5.4 消息处理流程

```
1. JSON 反序列化 Payload
2. 校验必填字段（EventID, Sha1, OssKey）
3. Inbox 幂等检查（三阶段）
4. 检查 OSS 对象是否已存在（MinIOObjectExists）
5. 不存在则上传（MinIOUploadBytes）—— 仅 EventType=="LoadFile"
6. 成功：
   - UPDATE files SET status="success" WHERE sha1=?
   - UPDATE inbox SET status=DONE
7. 失败：
   - UPDATE inbox SET status=FAILED, last_error=errMsg
   - 不提交 Kafka offset → 触发重投递
```

### 5.5 本地文件存储（Hash 分片）

```
路径结构：storage/{hash[0:2]}/{hash[2:4]}/{hash}
示例：storage/ab/cd/abcd1234567890...

两级目录分片，防止单目录 inode 耗尽。
删除文件后尝试清理空的父目录（忽略错误）。
```

---

## 六、Email 服务（C++）

### 6.1 架构

```
Kafka Consumer → EmailService 任务队列 → Worker 线程池 → SMTP 发送
                                            ↓
                                     Redis 去重 + 验证码存储
```

### 6.2 核心组件

**Kafka Consumer（librdkafka）：**
- 订阅邮件 topic，自动提交
- 解析邮件地址（处理 %40 URL 编码 → @）
- 入队到 EmailService

**Redis 连接池（hiredis）：**
- 线程安全连接池，默认 8 个连接
- 获取连接时健康检查，失败自动重连

**EmailService（线程池）：**
- 默认 8 个 Worker 线程
- mutex + condition_variable 同步的任务队列
- **去重机制：** Redis `SET NX` + TTL，防止短时间内重复发送
- 生成 6 位随机验证码，存入 Redis（带 TTL）

**SMTP 发送（libcurl）：**
- 支持 text/plain 和 text/html
- SMTP AUTH LOGIN 认证
- SSL/TLS 加密

**配置热更新：**
- Nacos 动态配置（SMTP 凭证、Redis、Kafka、TTL 等）
- ConfigListener 模式支持热加载

---

## 七、AI 服务（Go gRPC）

### 7.1 接口

```protobuf
service AIService {
    rpc AIrequest(AIReq) returns (AIResp);
}
message AIReq {
    string username, userid, filename, filehash, file_size, query;
}
message AIResp {
    int32 code; string message; string data;
}
```

### 7.2 Gateway 端处理

- 支持 JSON body 或 query 参数
- AI 响应支持三种 action：`redirect`（跳转）、`json`（结构化数据）、`text`（纯文本）

---

## 八、MCP Server（Model Context Protocol）

### 8.1 传输方式

SSE（Server-Sent Events），基础路径 `/mcp`，Keep-Alive 15 秒。

### 8.2 认证

从 HTTP 头提取用户信息：`X-User-Id`, `X-Username`，注入到 Context。

### 8.3 工具列表

| 工具名 | 功能 | 调用的 gRPC 服务 |
|--------|------|-----------------|
| `account_userinfo` | 获取用户信息 | account_srv |
| `file_show` | 获取文件预览 URL | file_srv |
| `file_download` | 获取文件下载 URL | file_srv |
| `file_query` | 列出用户所有文件 | file_srv |
| `file_resolve_hash` | 根据文件名获取 SHA1 | file_srv |

### 8.4 服务发现

通过 Consul 发现 account_srv 和 file_srv，gRPC 连接超时 5 秒，请求超时 15 秒。

---

## 九、Go Internal 基础组件

### 9.1 配置中心（Nacos + Viper）

```
Nacos Server: 192.168.149.128:30848
Namespace: ce99961c-...
DataId: clouddisk.json, Group: dev

加载流程：
1. 创建 Nacos ConfigClient
2. GetConfig() 获取 JSON 配置
3. Viper 解析到 ViperConfig 结构体
4. ListenConfig() 注册监听器，配置变更时热更新

初始化顺序：Redis → MySQL → Consul → Kafka → AliOSS → MinIO
```

### 9.2 Consul 服务注册

```go
// 单例模式（sync.Once）
// 健康检查：gRPC 原生健康检查，5 秒间隔，3 秒超时
// 30 秒 Critical 状态后自动注销
// 服务发现：随机负载均衡
```

### 9.3 MinIO 客户端

- 端点规范化：支持 `http://host:port`、`https://host:port`、`host:port` 三种格式
- 自动创建 Bucket（幂等，处理竞态条件）
- 完整的分片上传支持：Init → PutPart → Complete/Abort
- 预签名 URL：上传（PUT）和下载（GET）
- `MinIOObjectExists` 区分 "NoSuchKey"/"NotFound" 和真正的错误

### 9.4 Kafka 生产者

```
同步生产者（等待 Broker 确认）
RequiredAcks: WaitForAll（所有副本确认）
Retry.Max: 3
消息 Key: SHA1（用于分区分布）
序列化: JSON
```

### 9.5 数据库初始化

```
两阶段连接：
1. 连接 MySQL 系统库，CREATE DATABASE IF NOT EXISTS orm_test
2. 连接 orm_test 库
3. AutoMigrate: Account, File, UserFile, Outbox, Inbox
慢查询阈值：1 秒
```

---

## 十、React 前端

### 10.1 技术栈

React 19 + TypeScript + Vite 6，Tailwind CSS，无状态管理库。

### 10.2 认证流程

```
signin → 用户名+密码 → 获取 JWT → localStorage('oss_token')
signup → 用户名+邮箱+密码 → 进入验证模式
verify → 邮箱+验证码 → 跳转登录
```

### 10.3 分片上传（UploadManager 组件）

```
1. 选择文件 → 计算整文件 SHA-256
2. 文件 < 5MB：简单上传 /file/upload
3. 文件 >= 5MB：
   a. /file/initupload → 获取 upload_id, part_size, total_parts
   b. 逐片上传：
      - file.slice(start, end) 切片
      - 计算分片 SHA-256
      - POST /file/uploadpart（自定义 Header：X-Upload-Id, X-Part-Number, X-Chunk-Hash）
      - 更新进度条：(part_number / total_parts) * 100%
   c. /file/CompleteMultipart 完成
```

### 10.4 主题系统

- Dark/Light 双主题，CSS 变量驱动
- `data-theme` 属性切换，localStorage 持久化
- 视觉效果：毛玻璃（backdrop-blur）、霓虹边框、扫描线动画

### 10.5 API 层

`ApiService` 单例类，所有请求通过 `fetch()`，自动附加 `Authorization: Bearer {token}`。

---

## 十一、数据库模型

### 核心表关系

```
Account (1) ←→ (N) UserFile (N) ←→ (1) File
                                         ↑
                                    SHA1 唯一索引（去重）

Outbox → Kafka → Inbox（幂等消费）
```

### 关键索引

| 表 | 索引 | 类型 |
|----|------|------|
| Account | Name | 唯一 |
| Account | Email | 唯一 |
| Account | Mobile | 普通 |
| File | Sha1 | 唯一 |
| UserFile | (AccountID, FileID, Name) | 联合唯一 |
| Outbox | EventID | 唯一 |
| Outbox | (Status, NextRetryAt) | 复合（调度查询） |
| Outbox | (LockedBy, LockedUntil) | 复合（分布式锁） |
| Inbox | EventID | 主键 |

---

## 十二、面试高频考点总结

### 分布式事务
Outbox 模式：业务数据和消息在同一个 MySQL 事务中写入，Dispatcher 异步投递到 Kafka，保证最终一致性。

### 幂等消费
Inbox 表 + 三阶段锁获取 + instanceID 分布式锁，保证 exactly-once 语义。

### 文件去重
SHA1 哈希唯一索引 + UserFile 多对多关系，相同内容只存一份。

### 分片上传
Redis Hash 存储上传状态（24h TTL），临时文件避免内存膨胀，MinIO 原生 multipart API。

### 缓存策略
ARC 缓存（LRU+LFU 自适应）缓存服务发现结果，Ghost Cache 动态调整分区比例。

### Token 管理
Redis 有序集合实现白名单（按时间戳排序），每用户最多 2 个活跃 Token，超出自动踢掉最旧的。

### 服务发现
Consul 注册 + gRPC 健康检查，OpenResty Lua 定时拉取，Gateway ARC 缓存 + Round-Robin。

### 异步解耦
Kafka 消息驱动：文件上传（file_srv → store_srv）、邮件发送（gateway → email_srv）。

cmake_minimum_required(VERSION 3.10)
project(gateway CXX)

include(CheckIncludeFileCXX)

check_include_file_cxx(any HAS_ANY)
check_include_file_cxx(string_view HAS_STRING_VIEW)
check_include_file_cxx(coroutine HAS_COROUTINE)
if (NOT "${CMAKE_CXX_STANDARD}" STREQUAL "")
    # Do nothing
elseif (HAS_ANY AND HAS_STRING_VIEW AND HAS_COROUTINE)
    set(CMAKE_CXX_STANDARD 20)
elseif (HAS_ANY AND HAS_STRING_VIEW)
    set(CMAKE_CXX_STANDARD 17)
else ()
    set(CMAKE_CXX_STANDARD 14)
endif ()

set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

add_executable(${PROJECT_NAME} main.cc)

# ##############################################################################
# If you include the drogon source code locally in your project, use this method
# to add drogon 
# add_subdirectory(drogon) 
# target_link_libraries(${PROJECT_NAME} PRIVATE drogon)
#
# and comment out the following lines
find_package(Drogon CONFIG QUIET)
if (NOT Drogon_FOUND)
    find_package(Drogon QUIET)
endif ()
if (NOT Drogon_FOUND)
    message(FATAL_ERROR "Drogon was not found. Install Drogon or set Drogon_DIR/CMAKE_PREFIX_PATH to the directory containing DrogonConfig.cmake or drogon-config.cmake.")
endif ()
target_link_libraries(${PROJECT_NAME} PRIVATE Drogon::Drogon)
# ##############################################################################

# Link protobuf and gRPC for generated stubs used by controllers
find_package(Protobuf REQUIRED)
find_package(c-ares CONFIG)
if(NOT c-ares_FOUND)
    find_package(PkgConfig REQUIRED)
    pkg_check_modules(CARES REQUIRED libcares)
endif()
find_package(gRPC REQUIRED)
target_link_libraries(${PROJECT_NAME} PRIVATE protobuf::libprotobuf gRPC::grpc++ )
if (WIN32)
    target_link_libraries(${PROJECT_NAME} PRIVATE ws2_32)
endif ()
find_package(OpenSSL REQUIRED)
target_link_libraries(${PROJECT_NAME} PRIVATE OpenSSL::Crypto)

# Link libcurl (used by consul client) and Nacos SDK C++
find_package(CURL REQUIRED)
target_link_libraries(${PROJECT_NAME} PRIVATE CURL::libcurl)

# Link spdlog and fmt
find_package(spdlog REQUIRED)
find_package(fmt REQUIRED)
target_link_libraries(${PROJECT_NAME} PRIVATE spdlog::spdlog fmt::fmt)

# Nacos SDK C++ typically installs headers to /usr/local/include/nacos and lib to /usr/local/lib
find_library(NACOSCPP_LIB nacos-cli PATHS /usr/local/lib /usr/lib)
if (NACOSCPP_LIB)
    target_link_libraries(${PROJECT_NAME} PRIVATE ${NACOSCPP_LIB})
else()
    message(WARNING "Nacos C++ SDK library 'nacos-cli' not found. Ensure it's installed under /usr/local/lib.")
endif()

# librdkafka C++ bindings (used by KafkaProducer)
find_path(RDKAFKA_INCLUDE_DIR
          NAMES librdkafka/rdkafkacpp.h
          PATHS /usr/include /usr/local/include)
find_library(RDKAFKA_CPP_LIB
             NAMES rdkafka++
             PATHS /usr/lib /usr/local/lib)
find_library(RDKAFKA_C_LIB
             NAMES rdkafka
             PATHS /usr/lib /usr/local/lib)

if (RDKAFKA_INCLUDE_DIR)
    target_include_directories(${PROJECT_NAME} PRIVATE ${RDKAFKA_INCLUDE_DIR})
else()
    message(WARNING "librdkafka headers not found. Set RDKAFKA_INCLUDE_DIR manually if installed in a non-standard location.")
endif()

if (RDKAFKA_CPP_LIB AND RDKAFKA_C_LIB)
    target_link_libraries(${PROJECT_NAME} PRIVATE ${RDKAFKA_CPP_LIB} ${RDKAFKA_C_LIB})
else()
    message(FATAL_ERROR "librdkafka++ and/or librdkafka libraries not found. Please install librdkafka-dev (with C++ bindings).")
endif()

if (CMAKE_CXX_STANDARD LESS 17)
    message(FATAL_ERROR "c++17 or higher is required")
elseif (CMAKE_CXX_STANDARD LESS 20)
    message(STATUS "use c++17")
else ()
    message(STATUS "use c++20")
endif ()

aux_source_directory(controllers CTL_SRC)
aux_source_directory(filters FILTER_SRC)
aux_source_directory(plugins PLUGIN_SRC)
aux_source_directory(models MODEL_SRC)
aux_source_directory(application/common APP_COMMON_SRC)
aux_source_directory(infrastructure/runtime INFRA_RUNTIME_SRC)
aux_source_directory(infrastructure/mysql INFRA_MYSQL_SRC)
aux_source_directory(infrastructure/jwt INFRA_JWT_SRC)
aux_source_directory(infrastructure/redis INFRA_REDIS_SRC)
aux_source_directory(infrastructure/outbox INFRA_OUTBOX_SRC)
aux_source_directory(infrastructure/storage_control INFRA_STORAGE_CONTROL_SRC)
aux_source_directory(${CMAKE_CURRENT_SOURCE_DIR}/../internal INTERNAL_SRC)

# Logs source
set(LOGS_SRC ${CMAKE_CURRENT_SOURCE_DIR}/../logs/Logger.cpp)

# gRPC/protobuf generated sources for AI service client
set(AI_PROTO_DIR ${CMAKE_CURRENT_SOURCE_DIR}/../../proto/AI_srv)
set(AI_PROTO_SRCS
    ${AI_PROTO_DIR}/ai.pb.cc
    ${AI_PROTO_DIR}/ai.grpc.pb.cc
)

# Storage Control protobuf messages are generated now; gRPC C++ stubs are
# generated by scripts/project_start_scripts/generate_storage_control_proto.*
# when grpc_cpp_plugin is available in the build environment.
set(STORAGE_CONTROL_PROTO_DIR ${CMAKE_CURRENT_SOURCE_DIR}/../../proto/storage_control)
set(STORAGE_CONTROL_PROTO_SRCS
    ${STORAGE_CONTROL_PROTO_DIR}/storage_control.pb.cc
    ${STORAGE_CONTROL_PROTO_DIR}/storage_control.grpc.pb.cc
)

drogon_create_views(${PROJECT_NAME} ${CMAKE_CURRENT_SOURCE_DIR}/views
                    ${CMAKE_CURRENT_BINARY_DIR})
# use the following line to create views with namespaces.
# drogon_create_views(${PROJECT_NAME} ${CMAKE_CURRENT_SOURCE_DIR}/views
#                     ${CMAKE_CURRENT_BINARY_DIR} TRUE)
# use the following line to create views with namespace CHANGE_ME prefixed
# and path namespaces.
# drogon_create_views(${PROJECT_NAME} ${CMAKE_CURRENT_SOURCE_DIR}/views
#                     ${CMAKE_CURRENT_BINARY_DIR} TRUE CHANGE_ME)

target_include_directories(${PROJECT_NAME}
                           PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}
                                   ${CMAKE_CURRENT_SOURCE_DIR}/models
                                   ${CMAKE_CURRENT_SOURCE_DIR}/../..)
target_include_directories(${PROJECT_NAME}
               PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/../internal
                   ${CMAKE_CURRENT_SOURCE_DIR}/../logs
                   ${AI_PROTO_DIR}
                   ${STORAGE_CONTROL_PROTO_DIR})
target_include_directories(${PROJECT_NAME}
                PRIVATE /usr/local/include/nacos
                        /usr/include/nacos)
target_include_directories(gateway PRIVATE
    /home/lihaoqian/Tools/jwt-cpp-master/include
)
target_sources(${PROJECT_NAME}
               PRIVATE
               ${SRC_DIR}
               ${CTL_SRC}
           ${FILTER_SRC}
           ${PLUGIN_SRC}
           ${MODEL_SRC}
           ${APP_COMMON_SRC}
           ${INFRA_RUNTIME_SRC}
           ${INFRA_MYSQL_SRC}
           ${INFRA_JWT_SRC}
           ${INFRA_REDIS_SRC}
           ${INFRA_OUTBOX_SRC}
           ${INFRA_STORAGE_CONTROL_SRC}
           ConsulRegister.cpp
           ${INTERNAL_SRC}
           ${LOGS_SRC}
           ${AI_PROTO_SRCS}
           ${STORAGE_CONTROL_PROTO_SRCS})
# ##############################################################################
# uncomment the following line for dynamically loading views 
# set_property(TARGET ${PROJECT_NAME} PROPERTY ENABLE_EXPORTS ON)

# ##############################################################################

add_subdirectory(test)
